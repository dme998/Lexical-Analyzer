#include "scanner.h"
#include "token.h"

/**
 * returns one token 
 * @param filename name of file to be run through scanner
 * @return token
 */
token_t lex(const std::string &filename) {
  token_t token;

  return token;
}
